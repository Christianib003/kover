project_name: Kover
seed: 42
paths:
  raw_dir: data/raw/insuranceqa
  processed_dir: data/processed
  checkpoints_dir: results/checkpoints
  logs_dir: results/logs
  plots_dir: results/plots
model:
  name: google/flan-t5-small
  max_source_len: 256
  max_target_len: 128
train:
  batch_size: 8
  num_epochs: 3
  learning_rate: 2.0e-5